/**
 * Backend service skeleton for Recon-OS.
 *
 * Reserved for the API service. No endpoints, handlers, or middleware are
 * implemented in this phase. The package exposes its identity and the server
 * contract so the directory boundary exists for later work.
 */

export const API_NAME = "@recon-os/api";
export const API_VERSION = "0.0.0";
