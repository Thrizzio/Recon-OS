/**
 * Type of a Document (e.g., PDF, HTML, MARKDOWN).
 * Enum representing a fixed set of document types.
 */
export enum DocumentType {
  PDF = "PDF",
  HTML = "HTML",
  MARKDOWN = "MARKDOWN",
  TEXT = "TEXT",
  CSV = "CSV",
  JSON = "JSON",
  IMAGE = "IMAGE",
  OTHER = "OTHER",
}
